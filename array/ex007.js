const nomes = ["Julia", "Breno", "Ana"];

for(let i = 0; i < nomes.length; i++){
    console.log(nomes[i]);
}

console.log('-----------')

for(let i = nomes.length - 1; i >= 0; i--){
    console.log(nomes[i]);
}